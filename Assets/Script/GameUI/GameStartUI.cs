// GameStartUI.cs (สั้นๆไว้กดจากปุ่ม)
using UnityEngine;

public class GameStartUI : MonoBehaviour
{
    public void PressStart()
    {

        // TODO: เปิดสปอว์นรถ / รีเซ็ตสกอร์ ฯลฯ
        // ScoreManager.I?.ResetAll();
        // spawner.enabled = true;
    }
}
